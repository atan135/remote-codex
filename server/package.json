{
  "name": "@remote-codex/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@remote-codex/shared": "workspace:*",
    "ws": "8.18.3"
  },
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --project tsconfig.json --noEmit && tsc --project ../integration/tsconfig.json --noEmit",
    "test": "vitest run --coverage",
    "test:without-e2e": "vitest run --coverage --exclude src/end-to-end.test.ts",
    "build": "tsc --project tsconfig.json"
  },
  "devDependencies": {
    "@types/ws": "8.5.14",
    "selfsigned": "2.4.1"
  }
}
