{
  "name": "@remote-codex/server-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "remote-codex-server": "./dist/cli-main.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "dependencies": {
    "@remote-codex/ops": "workspace:*",
    "@remote-codex/server": "workspace:*"
  },
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "pretest": "corepack pnpm --filter @remote-codex/shared build && corepack pnpm --filter @remote-codex/server build && corepack pnpm --filter @remote-codex/ops build",
    "test": "vitest run --coverage",
    "build": "tsc --project tsconfig.json"
  }
}
